apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: my-network-policy
  creationTimestamp: null
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network: "true"
  podSelector:
    matchLabels:
      io.kompose.network: "true"
